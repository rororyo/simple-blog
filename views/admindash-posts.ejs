<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="./styles/admindash.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css">
</head>
<body style="background-color: whitesmoke;">
    <%- include('navbar.ejs') %>  
    <a href="/admin-dashboard">User list</a>
    <div class="d-flex mt-5 align-items-center vh-100 flex-column">
        <h1>Simple Blog Posts</h1>
        <a href="/new" class="btn btn-success vw-100">Add a post</a>
        <table class="table table-striped table-bordered table-hover table-responsive">
            <thead>
                <tr>
                    <th scope="col">No</th>
                    <th scope="col">Category</th>
                    <th scope="col">Title</th>
                    <th scope="col">Post Content</th>
                    <th scope="col">Author</th>
                    <th scope="col">Created at</th>
                    <th scope="col">Action</th>
                </tr>
            </thead>
            <tbody>
                <% for (let i = 0; i < posts.length; i++) { %>
                    <tr>
                        <th scope="row"><%= i + 1 %></th>
                        <td><%= posts[i].category_name%></td>
                        <td><%= posts[i].title %></td>
                        <td ><img src="./uploads/<%= posts[i].picture %>" alt="article image" class="table-img"></td>
                        <td><%= posts[i].author %></td>
                        <td><%= posts[i].date_created %></td>
                        <td><a href="/edit/<%= posts[i].id %>" class="btn btn-primary">Edit</a> <a href="/delete/<%= posts[i].id %>" class="btn btn-danger">Delete</a></td>
                    </tr>
                <% } %>
            </tbody>
        </table>
    </div>
    <%- include('footer.ejs') %>

</body>
</html>